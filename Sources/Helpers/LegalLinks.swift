import Foundation

enum LegalLinks {
    /// Live pages from TrackerBet (name-adjacent bet tracker) — replace with owned pages when available.
    static let privacy = URL(string: "https://www.trackerbet.net/privacy/")!
    static let terms = URL(string: "https://www.trackerbet.net/terms/")!
    static let support = URL(string: "https://www.trackerbet.net/")!
}
